# Copy this file to .env and adjust for your local setup.
#
# The defaults below match the docker-compose postgres service,
# so `./run.sh dev` works without any changes unless you run
# a different local Postgres instance.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/algosolver
